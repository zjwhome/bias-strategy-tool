# .impeccable.md — 设计上下文

> 本文件供 AI 设计/前端工作读取。修改界面外观前请先读这里。
> 最后更新：2026-09-15

## Design Context

### Users

**主要使用者**：本项目作者本人。跨境电商运营从业者，**非技术背景**，不写代码。
**使用情境**：每个交易日收盘后（约 15:30 之后）打开本地工具站看一次，约 1~2 分钟；
偶尔在盘中（14:30 前后持有股票时）再看一次确认是否需要卖出。
**要完成的任务**：用最快速度回答一个问题 —— **「今天要不要动手？」**
- 要动手 → 拿到买入清单，登记持仓
- 不用动手 → 安心关掉页面，不再纠结

**次要使用者**：作者分享给的朋友（同样是零基础用户，自行克隆仓库到 Windows 本地运行）。
他们的数据是空的、持仓是空的 —— **空状态必须能自解释**。

**关键洞察**：这套策略是「守株待兔」，六年只有约 22 个可动手的交易日 ——
**页面 95% 的时间处于「不用动手」的安静状态**。所以安静状态才是主界面，必须被设计成
「有意为之的安心」而不是「什么都没有」。而真正该动手的那 5%，要一眼跳出来。

### Brand Personality

**三个词**：克制、可信、精密（restrained / trustworthy / precise）

- 语气：直接、不煽动、不承诺收益。把「不构成投资建议」当作品格而不是免责套话。
- 情绪目标：**平静**（不该动手时）与**笃定**（该动手时）。绝不制造 FOMO。
- 反面参考（明确不要）：
  - ❌ 券商 App 的红色大促销风、涨停敢死队风、K 线满屏闪烁
  - ❌ SaaS 落地页风：全屏渐变 Banner + 大号插画 + 「立即注册」按钮
  - ❌ 通用 AI 审美：紫蓝渐变、霓虹发光、毛玻璃滥用、万物皆卡片
  - ❌ 深色背景 + 荧光青绿的「伪黑客/伪终端」

### Aesthetic Direction

**方向：专业数据感（浅色为主，深色可选）**

参照感觉：Bloomberg / 同花顺 专业版 / 券商研报的数据页 —— 而不是消费级理财 App。
核心是**排版的精度**而不是装饰的强度。

- **主题**：浅色为默认，**必须同时提供深色**，右上角三档切换（浅色 / 深色 / 自动）。两套都需单独调好。
- **色彩空间**：用 `oklch()`（已实测当前浏览器支持）。中性色带极轻的蓝相（chroma ≈ 0.005~0.03），
  避免纯灰 / 纯黑 / 纯白。深色模式不用阴影做层次，改用「更亮的表面色」。
- **色彩纪律（60-30-10）**：强调色是稀缺资源。
  - 顶栏**不用**饱和渐变铺底（旧版是全宽蓝色渐变，属过度用色，本次移除）。
  - **唯一允许大面积高饱和色的地方＝「今天该动手」的结论块**。其余时间页面是安静的。
- **涨跌配色**：遵循国内惯例 **涨 = 红 / 跌 = 绿**，不可反转。
  数字方向除颜色外**还必须带 +/− 符号**，不能只靠颜色传达（红绿色盲约占男性 8%）。
- **字体**：中文用系统栈（PingFang SC / Microsoft YaHei），这是正确选择而非偷懒；
  等宽字体**只**用于两处：hero 大数字、股票代码 —— 不用来伪造「技术感」。
- **数字**：所有数值列 `font-variant-numeric: tabular-nums`，保证纵向对齐。
- **尺寸体系**：App 类界面用**固定 rem 阶梯**（不用流体 clamp），
  只有 hero 大数字用 clamp。字号少而对比强，避免 13/14/15/16 这种「泥泞层级」。

### Design Principles

1. **先回答「今天要不要动手」** —— 最重要的信息必须在首屏第一眼看到，不靠滚动。
2. **安静就真的安静，该响就大声响** —— 未达标状态用中性色克制表达；
   达标状态才允许用高饱和色。视觉强度必须反映策略状态，不能恒定喧闹。
3. **空状态是主界面** —— 「今天不用动手」「还没有持仓」出现频率最高，
   必须自带解释与安心感，而不是一句灰色的「无数据」。
4. **数字要精密，文案要人话** —— 表格对齐、小数位一致、单位明确；
   同时不堆术语，面向零基础用户写人能看懂的中文。
5. **不靠颜色单独传达信息** —— 涨跌带符号、状态带文字标签、错误带说明。
6. **零外部依赖** —— 不引 CDN、不引 web font、不引第三方图表库。
   断网、离线、单文件双击都要能正常显示。所有视觉必须靠原生 CSS/SVG/Canvas 实现。
