# 工具站 · 开发完成报告

> 完成日期：2026-09-15　|　运行方式：本机 `127.0.0.1:8000`　|　成本：¥0
> 本工具仅提供数据参考，**不构成投资建议**。

> ⚠️ **界面已于 2026-09-17 重构，本文中"任务中心 / 今日结论"等旧导航描述已过时。**
> 现行结构：**① 盘前结论 / ② 盘中参考 / ③ 盘后总结 / 我的持仓 / 历史走势**，
> 结论与它的任务合并到同一页（任务设置与历史收进页底折叠区），顶栏改为**四套配色**切换。
> 看当前界面请以 `工具站-使用手册.md`、`使用文档.md` 为准（本文保留为开发过程记录）。

---

## 一、结论先行

| 问题 | 答案 |
|---|---|
| 工具站做好了吗？ | ✅ 阶段 1–5 + 本轮三项需求全部完成 |
| 数据准吗？ | ✅ 本机引擎与六年回测口径**逐条一致**（校验见第四节） |
| 要花钱吗？ | ❌ 不要。不上云、不买服务器，全部在本机 |
| 每次怎么用？ | 双击桌面「乖离率策略控制台」→ 按 1 启动 → 网页看红绿灯 |
| 任务会自动跑吗？ | ❌ **不会。** 三个任务默认全部关闭，只在**你手动开启定时**后才会自动触发 |

---

## 二、交付清单

### 2.1 外部操作界面（本轮新增）

| 文件 | 作用 | 你要不要碰 |
|---|---|---|
| **桌面「乖离率策略控制台」** | 双击 → 图形化文字菜单（启动/关闭网站、跑任务、开手册） | ⭐ 每次用 |
| `webapp/控制台.bat` | 上面那个快捷方式实际指向的入口（纯 ASCII，规避中文路径问题） | 不用碰 |
| `webapp/control.py` | 控制台程序本体（启停、状态、任务、快捷方式） | 不用碰 |
| `webapp/app.ico` | 桌面图标（脚本现场生成，无需 Pillow） | 不用碰 |

**控制台菜单**：
```
[1] 启动工具网站    [2] 关闭工具网站    [3] 浏览器打开网站
[4] 立即执行任务（盘前/盘中/盘后）
[5] 打开使用手册    [6] 打开数据文件夹   [7] 创建/修复桌面快捷方式
```

### 2.2 程序文件（都在 `webapp/` 里）

| 文件 | 作用 |
|---|---|
| `server.py` | 后端服务（Flask，含**用户可控调度器**） |
| `tasks.py` | **任务引擎**（盘前/盘中/盘后 + 持仓体检） |
| `strategy_core.py` | 策略核心引擎（广计算 + 实时行情） |
| `updater.py` | 数据更新器（拉数据 + 算广度 + 存库） |
| `db.py` | 本地数据库（SQLite：广度/候选股/持仓/任务记录/任务设置） |
| `webui/index.html` | 网页界面（含**任务中心**） |
| `data.db` | 你的数据 |
| `server_console.log` | 网站运行日志（排错时看） |

### 2.3 文档

| 文件 | 作用 |
|---|---|
| `工具站-使用手册.md` | **傻瓜手册**，照着做就行 |
| `工具站-开发完成报告.md` | 本文件（技术说明） |
| `傻瓜版-超跌抄底策略.md` | 策略的大白话版 |
| `A股乖离率超跌反弹策略说明书.md` v2.1 | 策略专业版（含实盘落地章节） |
| `每日广度过检-操作卡.md` | 每日操作卡 + 记录表（手工备用） |
| `strategy_config.json` v2.1 | 机器可读参数（改门槛就改这里） |

---

## 三、★ 本轮三项需求的落地情况

| 需求 | 落地方式 | 验收 |
|---|---|---|
| **1. 桌面快捷图标 + 外部操作界面** | 桌面「乖离率策略控制台」→ 控制台文字菜单：启停网站、跑任务、开手册、开数据夹、修复快捷方式 | ✅ 快捷方式已创建（2036 字节），启停闭环实测通过 |
| **2. 任务由用户决定跑不跑（立即 / 定时）** | 任务中心每个任务一个「立即执行」+ 一个「定时开关」；**默认全关**；旧的后台自动计划任务已卸载 | ✅ 三个任务默认 `enabled=0`；调度器只处理用户开启的 |
| **3. 盘前 / 盘中 / 盘后三个基础任务** | 见下表 | ✅ 三个任务全部实测通过 |

### 三个任务的详细设计

| 任务 | 建议时间 | 耗时 | 输出什么 | 实测结果 |
|---|---|---|---|---|
| 🌅 **盘前任务** | 09:10 | 几秒 | 今日操作清单：卖什么 / 买什么 / 还是不动 | ✅ 0.1s 完成，逻辑正确 |
| ⏱ **盘中任务** | 14:30 | ~1 秒 | 持仓实时预警（+6% / +10% / -6% / 回撤 3%） | ✅ 0.5s，取到当日实时价 |
| 🌙 **盘后任务** | 15:35 | 15–25 分钟 | 下载全市场 → 今日广度 → 明日操作计划 | ✅ 广度/候选股/持仓体检全部正确 |

### 关键设计：为什么「网站不自己定时」很重要

- 旧版用 Windows 计划任务在 15:35 自动跑 —— 你**不知情**时系统就在动了。
- 新版把定时器**搬进工具站进程**，且**默认关闭**：
  - 你必须先在网页上打开开关，它才会到点触发；
  - 关掉开关 = 永远不自动跑；
  - 电脑关机 / 网站没开 = 定时器不工作（**故意如此**，避免开机后突然自动执行）。
- 所有执行（无论立即还是定时）都会写进 `task_runs` 表，**可查历史**。

---

## 四、★ 口径校验（最重要的一节）

「本机算出的广度 50 到底等不等于回测里胜率 85.6% 的那个 50？」
用本机引擎复算全历史（2021-01-04 ~ 2026-08-31，5010 只有效数据），与既有回测结论比对：

| 校验项 | 本机引擎 | 回测结论 | 判定 |
|---|---|---|---|
| 2026-03-23 单日广度 | **199** | 199 | ✅ 完全一致 |
| 2025-04 峰值广度 | 757 | 758 | ✅ 差 1 |
| 2024-02 峰值广度 | 465 | — | ✅ 量级一致 |
| 2022-04 峰值广度 | 222 | — | ✅ 量级一致 |
| 2026-09-14 今日广度 | **0**（乖离率命中 33 只） | 连接器独立测得「33 只、广度 0」 | ✅ 两条独立技术路线完全一致 |
| 六年广度≥50 天数 | 20 | 22 | ⚠️ 差 2 |

**差异是怎么来的（如实说明）：**

1. **幸存者偏差**：本机清单 5017 只 = *当前仍在市* 的股票；回测用 4993 只 = *含已退市* 的股票。
   已退市股票在退市前往往连续暴跌，会贡献信号日。少掉的 2 天就来自这里。
   → 但这恰恰是**实盘的本来面目**：你不可能买到已退市的股票。所以本机口径**更保守、更可信**。
2. **次新股**：24 个缓存文件对应的股票上市不足 60 个交易日，被「上市≥60日」规则自动排除，不影响广度。

**结论：口径成立。门槛 50 可直接沿用，对应六年胜率约 85.6%。**

---

## 五、技术选型（为什么这么搭）

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | Python + Flask | 只多装 1 个包，其余全用已装好的环境 |
| 数据库 | SQLite | Python 自带，零安装，单文件 |
| 前端 | 原生 HTML/CSS/JS + **手写 Canvas 图表** | 不依赖任何外网 CDN，断网也能看 |
| 控制台 | Python 标准库 + 批处理包装 | 不依赖 tkinter（本机 Python 精简版没有） |
| 实时行情 | 腾讯/新浪快照接口（**批量一次请求**） | 盘中任务只用 1 个 HTTP 请求 |
| 定时 | **进程内调度器（默认关闭）** | 执行权在你手上，不偷偷跑 |
| 快捷方式 | PowerShell COM（`WScript.Shell`）+ 脚本生成 ICO | 无需第三方库 |
| 成本 | **¥0** | — |

> 前端特意**不用 Chart.js**：若引外网 CDN，断网时曲线就画不出来。改成手写 Canvas 后完全离线可用。

---

## 六、开发过程中修掉的两个致命问题（如实记录）

### 问题 1：全量抓取会永久卡死（不修则每天更新都会挂）

- **现象**：跑到 2000/5017 就再也不动，CPU 恒定不再增长。
- **根因**：`akshare` 源码里所有网络请求**都没设超时**，个别连接永久挂死，
  导致线程池 `as_completed` 永远等不到结束。
- **修复**（三处）：
  1. 全局 `socket.setdefaulttimeout(20)`；
  2. 抓取阶段加**时钟兜底**（超时即放弃剩余任务，绝不死等）；
  3. `shutdown(wait=False, cancel_futures=True)`，不等待挂死线程。
- **验证**：原先 8 只「挂死」的股票修复后全部 **1~2 秒**抓取成功。

### 问题 2：控制台 `netstat` 输出解码崩溃

- **现象**：Windows 命令行工具输出是 GBK，用 `text=True`（utf-8）解码会在读取线程抛 `UnicodeDecodeError`。
- **修复**：统一按字节捕获后再做多编码尝试（utf-8 → gbk → latin-1）。
- **验证**：状态显示、启停、任务执行全部正常。

---

## 六之二、★ 全面代码审查与修复（2026-09-15 午间·第三轮）

对全部 7 个源文件（约 2900 行）逐行审查，共发现并修复 **16 个问题**，
其中 **4 个属于会实际伤害数据正确性的严重缺陷**。全部修复后跑了 **2 轮 25 项回归测试**，
以及一次「广度>0」完整链路验证，**全部通过**。

### A. 严重缺陷（已实证或必然触发）

| # | 问题 | 后果 | 修复 |
|---|---|---|---|
| 1 | **数据库已被污染**（实测） | daily 表有 **10 行**假数据（`stocks_total=50`），是早期 `--limit 50` 测试写进去的，会让广度曲线上出现错误空档 | 已删除这 10 行；并加写库前校验 |
| 2 | **测试模式会污染数据库** | `--limit N` 只跑部分股票，广度必然偏低，却照常写库；指定非交易日会写入「假广度 0」 | 加 `partial` 标记 + `stocks_total==0` 拦截，两种情况都跳过写库 |
| 3 | **调度器会"补跑"过期任务** | 晚上 22:00 打开网站，会把早上 09:10 的**盘前任务**也触发一次（盘前任务晚上跑毫无意义） | 加 60 分钟宽限窗口，超过即不再触发 |
| 4 | **失败任务被无限重试** | `has_run_since` 只认 'ok'，任务一旦失败，调度器**每 20 秒重试一次**，日志被刷爆 | 把 'fail' 也算作"已尝试过"，当天不再自动重试 |

### B. 逻辑与并发缺陷

| # | 问题 | 后果 | 修复 |
|---|---|---|---|
| 5 | **任务并发竞态** | `run_task_async` 的「检查 running」与「启动线程」不在同一把锁内 → 快速双击两次可让同一任务跑两遍 | 在同一锁内**原子占位**，失败时回滚 |
| 6 | **跨进程并发** | 控制台窗口与网页是**两个独立进程**，内存状态互不可见，可同时对同一任务发起执行 → 写同一批 CSV/数据库 | 新增 `db.has_running_task()`，用数据库记录做跨进程互斥；超 2 小时的记录视为僵尸并自动清理 |
| 7 | **持仓卖出优先级错误** | 「到期清仓（满 10 日）」会**无条件覆盖**更紧急的「跌破止损线」，页面提示误导 | 到期只在未触发其他条件时才赋值 |
| 8 | **SQLite 连接泄漏** | 标准库 `with conn` 只提交事务、**不会关闭连接**；服务长期驻留会不断堆积文件句柄 | 子类化 Connection，退出时真正 close |
| 9 | **并发写锁死** | 默认 5 秒超时，网页 + 调度器同时写会直接抛 `database is locked` | 连接超时提到 30 秒 + WAL 模式 |

### C. 健壮性与体验缺陷

| # | 问题 | 后果 | 修复 |
|---|---|---|---|
| 10 | **持仓录入无校验** | 买入价填 0 → 止损/止盈线全变 0 → 页面立刻误报「已达 +10% 清仓」 | 后端 + 前端双重校验（价格必须 > 0、代码 6 位、日期格式） |
| 11 | **平仓无校验** | 平仓价可传 0 | 同上校验 |
| 12 | **核心引擎缺列崩溃** | 若数据源变动导致缺 `amount` 列，整个更新会 KeyError 崩掉 | 加兜底并明确告警 |
| 13 | **前端日期用 UTC** | 北京时间早上 8 点前，"今天"会算成昨天 | 改用本地时间生成日期 |
| 14 | **前端空值崩溃** | 成交额/收盘价/BIAS 为 null 时 `toFixed()` 抛错，整块表格渲染失败 | 统一用 `fmt()` 兜底 |
| 15 | **前端串行加载脆弱** | 5 个加载器串行执行，任何一个接口出错，**后面全部不更新** | 每个加载器各自 try/catch |
| 16 | **控制台任务网页不可见** | 从控制台跑的任务，网页上看不到进度 | 进入页面时自动检测运行中任务并显示进度 |

### D. 验证方式与结果

| 验证 | 内容 | 结果 |
|---|---|---|
| 回归测试第 1 轮 | 连接关闭 / 防重试 / 僵尸清理 / 卖出优先级 / 写库防护 / API 校验 / 并发占位 / 计算正确性 | **25 项通过，0 失败** |
| 回归测试第 2 轮 | 同上（确认第二轮改动未引入新问题） | **25 项通过，0 失败** |
| 广度>0 链路验证 | 回看 2026-03-23（历史达标日）：选股 → 入库 → 5 个 API 序列化 | ✅ 广度 199、候选股 10 只、入库正常、API 全 200 |
| 数据口径复核 | 修复后重算：2026-09-14 广度 0、乖离率命中 33 只、门槛 50 | ✅ 与修复前一致（未改变口径） |
| 前端语法 + DOM 校验 | `node --check` + 全部 31 个 DOM 引用交叉核对 | ✅ 通过，无悬空引用 |
| 界面截图复核 | 无头 Chrome 截图 | ✅ 渲染正常，买入日期已显示本地日期 |

> **关键结论：本轮修复没有改变任何策略口径。** 2026-09-14 广度仍为 0、乖离率命中仍为 33 只、
> 2026-03-23 广度仍为 199 —— 修复的全部是「工程正确性」问题，不是策略逻辑问题。

---

## 七、已知限制（诚实边界，请务必读）

1. **收益大头来自崩盘月**。六年广度≥50 的 22 天里，2025-04 / 2024-02 / 2022-04 三个月占 44.4% 的信号。
   剔除后胜率仍有 61.5%，但平均收益只有 **+0.33%**。**请用 +0.33% 做日常预期，不要用 +4.37%。**
2. **机会极稀疏**。广度≥50 六年只有 22 天（约每年 3~4 次）。大多数日子「今天不动手」是**正常状态，不是故障**。
3. **2026 年至今偏弱**。峰值广度 199，胜率约 62.8%（仅 4 个达标日），因为今年没出现极端崩盘。
4. **数据源单一**：目前只用新浪源。若新浪改接口，需切换数据源（东财备用方案在本机被代理拦截，需另找）。
5. **止损用收盘价判定**，所以是「收盘跌破 -6% → 次日开盘卖」，不是盘中即时止损。
6. **不含停牌防护**：回测里的停牌防护用到了未来信息，实盘无法预知，已按诚实原则剔除。
7. **涨跌停无法买入**的情况无法完全模拟（如达标日次日一字涨停）。
8. **进程内定时不抗关机**：网站没开 / 电脑关机时，定时器不触发。这是**故意的**设计（见第三节）。
9. **盘后任务耗时较长**（15–25 分钟）：需下载全市场 5000+ 只股票的日线。建议只在收盘后跑一次。

---

## 八、日常使用（三句话）

1. **打开**：双击桌面「乖离率策略控制台」→ 按 **1**，浏览器自动打开。
2. **跑数据**：网页任务中心 → 「🌙 盘后任务」→ **立即执行**（等 15–25 分钟）。
3. **看结论**：灰色（< 50）→ 关掉，明天再来；红色（≥ 50）→ 看候选股，买完在「我的持仓」记账。

---

## 九、下一步建议

- [ ] **先跑一周观察**：每天打开看一眼，确认红绿灯和数字符合预期（大概率一直是灰色）。
- [ ] **模拟盘验证 ≥1 个月**：等真正出现红色达标日，按规则模拟买入，核对收益是否符合回测。
- [ ] **再考虑实盘**：且只用小仓位试。
- [ ] **想调门槛**：改 `strategy_config.json` 里 `regime_gate.presets.main.threshold`（50 → 100 → 150，胜率升、机会减）。
- [ ] **想省事**：在任务中心给「盘后任务」打开定时（如 15:35），这样每天收盘后自动更新。
