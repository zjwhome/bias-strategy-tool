# ============================================================
# 乖离率超跌反弹策略工具 · Git 忽略规则
# ============================================================

# ---------- 行情数据（体积巨大：合计约 2.2GB，且属于本机数据） ----------
# 这些目录是本地缓存/回测产物，克隆仓库后由程序自动重新生成
#
# ★ 唯一例外：data_long/_universe.csv 只有 100KB，是「回测口径股票清单」（5017 只）。
#   必须随仓库分发，否则别人克隆后只能退到交易所口径清单（4414 只），
#   算出来的「信号广度」与本项目的回测结论对不上（门槛 50 的语义会漂移）。
#   注意写法：必须先排除目录内容再单独放行该文件，直接写 data_long/ 会让 ! 规则失效。
data_long/*
!data_long/_universe.csv
data_full/
output_long/
output_full/

# ---------- 本地数据库与运行日志 ----------
webapp/data.db
webapp/data.db-wal
webapp/data.db-shm
webapp/server_console.log
*.log

# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ---------- 开发期临时探针脚本（下划线开头） ----------
_*.py

# ---------- WorkBuddy 工作区内部数据（含个人工作笔记） ----------
.workbuddy/

# ---------- 编辑器 / 操作系统 ----------
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
