# 工具站使用手册（傻瓜版）

> 一句话：**每天收盘后看一眼数字，数字够大才动手，不够大就关掉。**
> 所有任务都由你自己决定跑不跑、什么时候跑——**网站不会自作主张。**
> 不构成投资建议。

---

## 一、这工具是干嘛的

它每天帮你算出一个数字——**「今日广度」**。

广度 = 今天全市场有多少只股票同时满足「跌得够狠 + 放量 + 还在跌」。
这个数字越大，说明市场越恐慌，**超跌反弹的胜率越高**。

- 广度 **< 50** → 关掉页面，今天不动手（空仓也是操作）
- 广度 **≥ 50** → 页面会给你 10 只候选股 + 买卖规则

---

## 二、三种打开方式（任选）

### 方式 A：双击桌面图标（最推荐）

桌面上的 **「乖离率策略控制台」** —— 双击它，进入一个文字操作界面：

```
   乖离率策略 · 控制台　（本地运行，不联网上传）
──────────────────────────────────────────────────────
   工具网站： ○ 已停止          ← 一眼看到开没开
   数据日期： 2026-09-14　今日广度： 0 只（门槛 50）→ 未达标
   定时任务： 全部关闭（网站不会自己定时，需要你到网页上开启）
──────────────────────────────────────────────────────
   [1] 启动工具网站（自动打开浏览器）
   [2] 关闭工具网站
   [3] 用浏览器打开工具网站
──────────────────────────────────────────────────────
   [4] 立即执行任务（盘前 / 盘中 / 盘后）
──────────────────────────────────────────────────────
   [5] 打开使用手册
   [6] 打开数据文件夹
   [7] 创建 / 修复桌面快捷方式
   [0] 退出
```

- 按 **1** = 启动网站 + 自动开浏览器
- 按 **2** = 关掉网站（释放端口）
- 按 **4** = 不用开浏览器，直接在黑窗口里跑任务
- 找不到桌面图标了？按 **7** 重新创建

### 方式 B：直接在浏览器

网站已经在跑时，浏览器打开 **http://127.0.0.1:8000** 即可。

### 方式 C：命令行（一般用不到）

```
webapp\控制台.bat                  # 打开控制台
python webapp\control.py start     # 只启动网站
python webapp\control.py stop      # 只关闭网站
python webapp\control.py task premarket   # 只跑一个任务
```

---

## 三、每天怎么用（4 步）

### 第 1 步：启动

双击桌面「乖离率策略控制台」→ 按 **1** 启动网站。浏览器会自己打开。

### 第 2 步：跑「盘后任务」（更新数据）

这是最关键的一步，**每天收盘后跑一次**。

- **在网页上跑**：任务中心 → 找到「🌙 盘后任务」→ 点 **「立即执行」**
- **在控制台跑**：按 **4** → 选 **3**

要等 **15~25 分钟**（要下载全市场 5000 多只股票的数据）。

> 想看进度就盯着网页，进度条会实时走动（`1200/5017 成功 1200 失败 0`）。
> 跑的时候可以关掉网页，任务在后台继续，跑完结果会存在页面上。

### 第 3 步：看红绿灯

| 你看到 | 含义 | 你要做什么 |
|---|---|---|
| 🔴 红色大数字，≥ 门槛 | **达标，可以出手** | 看下面的候选股表格 |
| ⚪ 灰色大数字，< 门槛 | **未达标** | 关掉，明天再来 |

也可以直接看「盘后任务」的结论一句话：
- `2026-09-14 广度 0 未达标 → 明天不动手。`
- `2026-03-23 广度 199 达标 → 明天可以出手！`

### 第 4 步：按规则买卖（只有达标日才需要）

**买什么**：候选股表格里的股票（已按跌幅从大到小排好）

**怎么买**：
- 达标日的**下一个交易日**开盘买入
- 最多买 **10 只**，平均分配资金（例如 10 万本金，每只 1 万）
- 分 2~3 批买入，不要一次全仓

**什么时候卖**（四条红线，满足一条就卖）：
| 情况 | 动作 |
|---|---|
| 收盘价跌破买入价的 **-6%** | 次日开盘全部卖出（止损） |
| 涨到 **+6%** | 卖出一半 |
| 涨到 **+10%** | 全部卖出 |
| 最高曾涨过 +5%，之后从最高点回撤 **3%** | 全部卖出（保住利润） |
| 持满 **10 个交易日**还没触发上面任何一条 | 到期全部卖出 |

---

## 四、任务中心：三个任务，都由你决定跑不跑

网页中间的 **「任务中心」** 有三个任务。每个任务有两种执行方式，**都在你手上**：

| 按钮 | 作用 |
|---|---|
| **立即执行** | 点一下马上跑（想跑就跑） |
| **定时开关 + 时间** | 你**先手动打开**开关，到点才会自动跑（默认全部关闭） |

> ★ 重要：**网站本身绝不偷偷定时。** 开关是你的，关掉就永远不会自动跑。

### 🌅 盘前任务（建议 09:10）

**几秒完成**，不下载数据。开盘前跑一次，给你一张「今日操作清单」：

- 今天要**卖出**哪些持仓（触发了止损/止盈/到期）
- 今天要**买入**哪些候选股（如果昨天广度达标）
- 或者明确告诉你「**今天不买也不卖**」

### ⏱ 盘中任务（建议 14:30）

**约 1 秒完成**。只盯你自己的持仓，拉实时价格：

- 已到 **+6%** → 提示可卖一半
- 已到 **+10%** → 提示该清仓
- 已跌破 **-6%** → 提示预警
- 从最高点回撤 **3%** → 提示该走

> ⚠️ 盘中只是**预警**。策略规则以**收盘价**为准——不要因为盘中插针就慌。

### 🌙 盘后任务（建议 15:35）

**15~25 分钟**，最核心。下载全市场数据 → 算今日广度 → 给出结论：

- 达标 → 输出明天的**买入候选股**
- 不达标 → 明确告诉你「**明天不动手**」
- 同时完成**持仓体检**和「明日操作计划」

### 怎么设置定时

1. 在任务卡片上把 **定时开关** 打开
2. 填时间（如 `15:35`）和星期（默认周一到周五）
3. 点保存

> 想改回手动：把开关关掉即可。
> 建议只在「盘后任务」上开定时（15:35），另外两个按需手动跑。

### 看执行历史

任务卡片下方能展开**历史执行记录**，点开可看每次的完整结论。
结论会一直存着，随时回看。

---

## 五、持仓记录怎么用

买进后，在页面「我的持仓」里填一行：

| 填什么 | 说明 |
|---|---|
| 股票代码 | 6 位数字，如 `300274` |
| 名称 | 可以空着，会自动填 |
| 买入日期 | 选日历 |
| 买入价 | 实际成交价 |
| 股数 | 可不填 |

点「加入持仓」后，页面会自动帮你盯着：

- 止损线（买入价 × 0.94）
- 止盈线（× 1.06 / × 1.10）
- 持有天数
- **该不该卖了**（会直接显示「★ 明天开盘卖出」这种提示）

卖掉之后，点那一行右边的「删除」把它删掉即可。

---

## 六、数据从哪来 / 什么时候更新

- 数据来源：新浪财经免费接口，保存在你本机（`data_long` 文件夹）
- **更新时机：由你点击「盘后任务 → 立即执行」触发**
- 也可以在控制台按 **4 → 3** 触发

> ★ 这里和旧版本不同：**不再有 15:35 的后台自动任务。**
> 原因是你希望自己掌握执行权。想让它在 15:35 自动跑，就在任务中心把
> 「盘后任务」的定时开关打开——那是**你**开启的，随时能关。

> 如果页面显示的数据日期不是今天，说明还没更新。跑一次盘后任务即可。

---

## 七、常见问题

**Q1：黑窗口关了会怎样？**
A：页面就打不开了。重新双击桌面图标 → 按 1 启动即可。

**Q2：提示端口被占用 / 网站已经开着？**
A：说明网站已经在运行，直接在浏览器打开 `http://127.0.0.1:8000` 就行。
   想重启：控制台按 2 关闭，再按 1 启动。

**Q3：一直显示「今天不动手」，是不是坏了？**
A：不是。这个策略本来就是**守株待兔**——六年里只有 22 天达标（约每年 3~4 次）。
   大部分日子空仓等待，是策略的一部分，不是故障。

**Q4：任务跑到一半能关网页吗？**
A：可以。任务在后台跑，关掉浏览器不影响。跑完结果会存在「执行历史」里。

**Q5：能一直挂着自己跑吗？**
A：可以。网站开着就一直能用，不占资源。

**Q6：会上传我的数据吗？**
A：不会。所有数据都在你本机，不联网上传。

**Q7：定时开关开着，但电脑关机了会跑吗？**
A：不会。这个定时器住在工具站进程里，电脑关机 / 网站没开就不会触发。
   想「关机也能跑」需要系统级计划任务，目前故意没做——避免你不知情时被自动执行。

---

## 八、胜率是多少（如实说）

广度 ≥ 50 时，六年回测：

| 指标 | 数值 |
|---|---|
| 胜率 | **85.6%** |
| 平均收益 | +4.37% |
| 出手次数 | 六年 22 次（约每年 3.7 次） |

**但要诚实告诉你：**

- 收益的大头来自**崩盘事件月**（2025-04 / 2024-02 / 2022-04）。
- 如果剔除这三个月，胜率仍有 61.5%，但平均收益只有 **+0.33%**。
- 所以：**把它当成「大跌时的抄底工具」，不要当成「天天赚钱的工具」。**
- 2026 年至今（峰值广度 199），胜率约 **62.8%**——比六年平均低，因为今年没出现极端崩盘。

**结论：门槛越高胜率越高，但机会越少。想更稳，可以把门槛自己调到 100 或 150。**

---

## 九、免责声明

本工具仅提供数据参考，**不构成任何投资建议**。
股市有风险，任何策略都可能亏损，请自行决策并控制仓位。
