# 代码审查与修复报告

> 审查日期：2026-09-15　|　审查范围：`webapp/` 全部 7 个源文件（约 2900 行）
> 审查方式：逐行阅读 + 数据库实测诊断 + 2 轮回归测试 + 端到端链路验证
> 本工具仅提供数据参考，**不构成投资建议**。

---

## 一、结论先行

| 问题 | 答案 |
|---|---|
| 找到问题了吗？ | ✅ 共 **16 个**，其中 **4 个会实际伤害数据正确性** |
| 有已经发生的实际损害吗？ | ⚠️ **有**：数据库已被污染 10 行（已清理） |
| 都修好了吗？ | ✅ 全部修复，**2 轮 × 25 项回归测试全通过** |
| 修复会改变策略结论吗？ | ❌ **不会**。修复前后广度、候选股、门槛完全一致 |
| 现在能放心用吗？ | ✅ 可以。建议先空跑观察一周 |

---

## 二、最重大发现：数据库已经被污染了

**这不是"可能出问题"，而是"已经出问题了"。**

诊断发现 `daily` 表里有 **10 行假数据**：

| 日期 | 广度 | 全市场股票数 |
|---|---|---|
| 2025-08-20 ~ 2025-09-02（共 10 个交易日） | 0 | **50** ← 明显不对（真实值约 5000） |

**根因**：早期我做功能测试时用了 `updater.py --limit 50`（只跑前 50 只股票验证流程），
但代码**照常把这些"只统计了 50 只"的结果写进了数据库**。

**影响**：这 10 行会让网页上的广度曲线上出现一段错误空档，也让 `stocks_total` 字段失去可信度。

**处置**：
1. 已删除这 10 行污染数据（表恢复为 250 行干净数据）
2. **在写库前加了健全性校验**，从根上杜绝再次发生：
   - 测试模式（`--limit`）→ 跳过写库，只在屏幕上打印结果
   - 指定了非交易日 / 无数据 → 跳过写库并提示

> 附带一个可用的排查口径：**只要 `stocks_total < 4000`，就是异常行**。

---

## 三、4 个严重缺陷（会实际造成损害）

### 1. 测试模式污染数据库 ← 就是上面那个

| | |
|---|---|
| **触发** | 跑 `updater.py --limit 50`，或指定一个非交易日 |
| **后果** | 把"只统计了 50 只"的假广度写进库，污染历史曲线 |
| **修复** | 加 `partial` 标记 + `stocks_total==0` 拦截，两种情况都**不写库** |

### 2. 调度器会"补跑"过期任务

| | |
|---|---|
| **触发** | 你晚上 22:00 才打开工具网站 |
| **后果** | 系统会把**早上 09:10 的盘前任务**也触发一次——盘前任务晚上跑毫无意义，而且违背了你「由我决定何时执行」的要求 |
| **修复** | 加 **60 分钟宽限窗口**：只补跑刚错过不久的，超过 1 小时就不再触发 |

### 3. 失败的任务会被无限重试

| | |
|---|---|
| **触发** | 某个任务执行失败（比如网络断了） |
| **后果** | 调度器判断"今天还没跑成功"，于是**每 20 秒就重试一次**，日志被刷爆、数据库被反复写 |
| **修复** | 把「失败」也算作"已经尝试过"，当天不再自动重试；你想重跑可以手动点 |

### 4. 同一个任务可能被同时跑两遍

| | |
|---|---|
| **触发** | 快速双击两次「立即执行」按钮 |
| **后果** | 两次请求都通过了检查，同一任务被启动两次 → 两个线程同时写同一批文件 |
| **修复** | 在**同一把锁内**完成"检查 + 占位"，第二次必然被拒绝 |

---

## 四、其余 12 项修复

### A. 并发与数据层（3 项）

| 问题 | 后果 | 修复 |
|---|---|---|
| **数据库连接不关闭** | Python 标准库的 `with 连接` 只提交事务、**并不关闭连接**；本工具是长期运行的服务，会不断堆积文件句柄 | 自定义连接类，退出时真正关闭 |
| **并发写会报错** | 网页和后台任务同时写库时，默认 5 秒超时会直接抛 `database is locked` | 超时放宽到 30 秒 + 开启 WAL 模式 |
| **控制台与网页会打架** | 控制台窗口和网页是**两个独立程序**，互相看不见对方在干什么，可能同时跑同一个任务 | 用数据库记录做跨进程互斥；超过 2 小时的记录视为"僵尸"自动清理 |

### B. 逻辑错误（1 项）

| 问题 | 后果 | 修复 |
|---|---|---|
| **卖出提示优先级错了** | 「到期清仓（满 10 日）」会**盖掉**更紧急的「跌破止损线」。你会看到"到期清仓"，而实际上这只股票早就该止损了 | 到期只在没有其他信号时才显示；止损永远优先 |

### C. 健壮性与体验（8 项）

| 问题 | 后果 | 修复 |
|---|---|---|
| **持仓录入没有校验** | 买入价填 0 → 止损线和止盈线全变成 0 → 页面**立刻误报「已达 +10% 清仓」** | 前后端双重校验（价格必须 >0、代码 6 位数字、日期格式） |
| **平仓没有校验** | 平仓价可以传 0 | 同上 |
| **核心引擎缺列会崩** | 如果数据源改版导致缺 `amount` 列，整个更新直接崩溃 | 加兜底 + 明确告警 |
| **网页"今天"的日期会算错** | 用来生成日期的函数用的是**国际标准时间**，北京时间早上 8 点前会算成**昨天** | 改用本地时间 |
| **网页表格可能整块渲染失败** | 某个数值为空时，格式化函数抛错，整张表都显示不出来 | 统一兜底显示"—" |
| **一个接口出错拖垮整个页面** | 5 个数据模块是串行加载的，任何一个出错，**后面全部不刷新** | 每个模块各自独立容错 |
| **控制台跑的任务网页看不到** | 从控制台启动的任务，网页上不显示进度 | 进入页面时自动检测并显示进度 |
| 死代码 | 一处赋值后从未使用的变量 | 已清理 |

---

## 五、怎么验证的（不是"我觉得好了"，是"测过了"）

| 验证项 | 内容 | 结果 |
|---|---|---|
| **回归测试第 1 轮** | 连接关闭 / 防无限重试 / 僵尸清理 / 卖出优先级 / 写库防护 / API 校验 / 并发占位 / 计算正确性 | ✅ **25 项全过** |
| **回归测试第 2 轮** | 全部重跑（确认后续改动没引入新问题） | ✅ **25 项全过** |
| **广度>0 链路验证** | 回看历史达标日 2026-03-23：选股 → 入库 → 5 个接口返回 | ✅ 广度 **199**、候选股 **10 只**、接口全 200 |
| **前端语法 + 结构** | `node --check` 语法校验 + 31 个页面元素引用逐一交叉核对 | ✅ 通过，无悬空引用 |
| **界面截图复核** | 无头浏览器实际渲染截图 | ✅ 正常（买入日期已正确显示本地日期） |
| **数据口径复核** | 修复后重新计算 | ✅ 与修复前**完全一致** |

> 其中「广度>0 链路验证」尤其重要：**此前所有实测都是广度 0、候选股为空**，
> "选股 → 入库 → 接口返回"这条真正会用在实盘上的路径**从未被验证过**。这次补上了。

---

## 六、最重要的一句话：没有动策略

本轮修复的**全部是"工程正确性"问题，不是策略逻辑问题**。修复前后的关键数字：

| 校验项 | 修复前 | 修复后 |
|---|---|---|
| 2026-09-14 广度 | 0 | **0** |
| 2026-09-14 仅乖离率命中 | 33 只 | **33 只** |
| 2026-03-23 广度 | 199 | **199** |
| 门槛 | 50 | **50** |

**结论：门槛 50 依然对应六年胜率 85.6% 的那个 50，不需要做任何换算或重新标定。**

---

## 七、剩余风险（如实告知）

1. **回测口径含"未来信息"，实盘做不到**（持有期内停牌防护），已按诚实原则剔除。这是已知偏差，不是 bug。
2. **数据源单一**：只用新浪接口。若新浪改版，需要另找数据源（东财方案在本机被网络策略拦截）。
3. **进程内定时不抗关机**：电脑关机 / 网站没开时定时器不工作。这是**故意的**——避免你不知情时被自动执行。
4. **盘后任务耗时 15~25 分钟**：需要下载全市场 5000+ 只股票日线，属正常。
5. **幸存者偏差**：本机清单是"当前仍在市"的股票，比回测少 2 个达标日，方向更保守（实盘买不到退市股）。

---

## 八、建议的下一步

- [ ] **先空跑一周**：每天打开看一眼，确认一直是灰色（符合预期），顺便验证每日更新稳定
- [ ] **模拟盘验证 ≥1 个月**：等真正出现红色达标日，按规则模拟买入，核对收益
- [ ] **想调门槛**：改 `strategy_config.json` 里 `regime_gate.presets.main.threshold`（50 → 100 → 150）
- [ ] **想省事**：在任务中心给「盘后任务」打开定时（如 15:35）

---

*本报告为工程质量审查记录，所有内容仅作数据参考，**不构成投资建议**。*
