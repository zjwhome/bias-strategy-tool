# 开工前盘点与开发计划

> 盘点日期：2026-09-15　|　目标：在**本机**搭建一个每日参考用的乖离率策略工具站
> 结论：**地基是完善的，可以开工。** 缺口都在"应用层"，不在"策略层"。

---

## 一、结论先行

| 问题 | 答案 |
|---|---|
| 策略规则完整吗？ | ✅ 完整。说明书 v2.1 + 傻瓜版 + 操作卡，可直接执行 |
| 数据能力够吗？ | ✅ 够。**本机可独立获取全市场数据，不依赖任何付费接口** |
| 能复现回测口径吗？ | ✅ **能精确复现**（见第三节，这是最重要的一条好消息） |
| 缺什么？ | ⚠️ 缺"应用层"：数据自动更新、信号存档、网页界面、持仓记录 |
| 能不能开工？ | ✅ **可以，且不需要花钱、不需要上云** |

---

## 二、已有资产（盘点结果，全部 ✅）

| # | 资产 | 文件 / 位置 | 状态 |
|---|---|---|---|
| 1 | 策略规则（专业版） | `A股乖离率超跌反弹策略说明书.md` v2.1 | ✅ |
| 2 | 策略规则（大白话版） | `傻瓜版-超跌抄底策略.md` | ✅ |
| 3 | 每日操作卡 + 记录表 | `每日广度过检-操作卡.md` | ✅ |
| 4 | 机器可读参数 | `strategy_config.json` v2.1 | ✅ |
| 5 | 六年回测报告 | `回测报告-全量第二轮.md` | ✅ |
| 6 | 指标计算代码 | `backtest_bias_full.py`（BIAS / 均线 / 放量 / 5日跌 / 停牌防护） | ✅ |
| 7 | 全市场日线缓存 | `data_long/` **5,017 只**股票 | ✅ |
| 8 | Python 运行环境 | akshare / pandas / numpy / openpyxl 已装好 | ✅ |

---

## 三、★ 最重要的好消息：本机能精确复现回测口径

我用连接器算广度时，需要做口径校准（实盘口径 vs 回测口径，倍数 1.08）。
**但用本机数据算，不需要校准——因为口径完全一致。**

本机广度算法（与六年回测**逐条对应**）：

```
广度 = 当日全市场满足以下全部条件的股票数：
  ①  BIAS_24 ≤ -15%        （24日均线乖离率，前复权）
  ②  成交量 ≥ 5日均量 × 1.5  （放量）
  ③  近 5 日涨跌幅 < 0       （仍在下跌途中）
  ④  成交额 ≥ 8,000 万元      （流动性下限）
  ⑤  上市 ≥ 60 个交易日      （剔除次新）
  ⑥  持有窗口内无停牌         （停牌防护）
  ⑦  非 ST / 非退市 / 非北交所 （标的池）
```

**含义：本机算出的广度 50，直接等于回测里胜率 85.6% 的那个 50。不用换算、不用猜。**

---

## 四、地基实测结果（本次实跑）

| 测试项 | 结果 | 结论 |
|---|---|---|
| 全市场日线缓存 | 5,017 只 CSV | ✅ 已就绪 |
| **能否拉到最新交易日** | sh600519 返回最后日期 **2026-09-14** | ✅ **能，工具站可以每天更新** |
| 并发拉取（300只/12线程） | **52.1 秒，成功率 300/300 = 100%** | ✅ 稳定 |
| 全市场更新耗时 | **12 线程约 14.5 分钟；16 线程约 7.3 分钟** | ✅ 收盘后后台跑，可接受 |
| 数据一致性 | 300 只样本的最后交易日**全部**是 2026-09-14 | ✅ 数据源可靠 |
| 股票清单（ST 过滤用） | 上交所 1,701 + 深交所 2,901，含上市日期 | ✅ 可用 |
| 全市场快照接口 | 会挂起（不可用） | ⚠️ 不用它，用逐只拉取方案 |

**结论：每日更新一次全市场数据约 15 分钟，完全可接受。**

---

## 五、需要补的缺口（6 项，都是"应用层"）

| # | 缺口 | 怎么做 | 难度 |
|---|---|---|---|
| 1 | **数据自动更新器** | 复用回测脚本的拉取逻辑，支持"只更新最新几天" | 低 |
| 2 | **每日信号计算 + 存档** | 复用 `compute_features`，把每天的广度和候选股写入数据库 | 低 |
| 3 | **本地数据库** | SQLite（Python 自带，零安装）：存信号历史 + 我的持仓 | 低 |
| 4 | **后端接口** | Python + Flask，把数据吐给网页 | 低 |
| 5 | **网页界面** | 原生 HTML + Chart.js，不用任何构建工具 | 中 |
| 6 | **一键启动 + 定时任务** | 双击 .bat 启动；Windows 任务计划自动更新 | 低 |

---

## 六、工具站方案设计

### 6.1 整体样子

```
┌─────────────────────────────────────────────────────────┐
│  📊 乖离率策略 · 每日参考                                 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│   今日广度                                               │
│   ┌────────┐                                            │
│   │   0    │   ⛔ 未达门槛（需要 ≥ 50）                   │
│   └────────┘   动作：今天不动手                            │
│                                                          │
│   数据日期：2026-09-14    更新时间：今天 15:42             │
├─────────────────────────────────────────────────────────┤
│   历史广度走势（近 1 年曲线 + 门槛虚线）                    │
├─────────────────────────────────────────────────────────┤
│   候选股（仅广度达标时显示）                               │
│   代码    名称   BIAS   换手   量比   5日跌                │
│   ...                                                    │
├─────────────────────────────────────────────────────────┤
│   我的持仓（买入价 / 止损线 / 止盈线 / 持有天数）            │
└─────────────────────────────────────────────────────────┘
```

### 6.2 技术选型（全部本地、全部免费）

| 层 | 选型 | 理由 |
|---|---|---|
| 后端 | **Python + Flask** | 只多装 1 个包；你机器上已有 Python 环境 |
| 数据库 | **SQLite** | Python 自带，无需安装 |
| 前端 | **原生 HTML/CSS/JS + Chart.js** | 不用 npm、不用打包、不用编译 |
| 访问 | 浏览器打开 `http://127.0.0.1:8000` | 只在本机，不对外，安全 |
| 定时 | **Windows 任务计划程序** | 系统自带 |
| 成本 | **¥0** | 不买服务器、不买域名、不买数据库 |

### 6.3 每日使用流程（目标：傻瓜化）

```
15:30 收盘
   ↓
【自动】后台更新数据（约 15 分钟，你不用管）
   ↓
【自动】计算广度 + 候选股，存入数据库
   ↓
你打开浏览器 → 看到今天的广度和该做什么
   ↓
广度 < 50 → 关掉，明天再来
广度 ≥ 50 → 按页面提示的 10 只股票 + 仓位规则操作
```

### 6.4 目录结构（规划）

```
股票工具/
├── data_long/                  # 日线缓存（已有）
├── webapp/                     # ← 工具站（新建）
│   ├── server.py               # 后端 + 网页
│   ├── updater.py              # 数据更新 + 信号计算
│   ├── strategy_core.py        # 策略核心（从回测脚本抽取）
│   ├── webui/
│   │   ├── index.html          # 首页看板
│   │   ├── history.html        # 历史广度
│   │   └── holdings.html       # 我的持仓
│   ├── data.db                 # SQLite 数据库
│   ├── 一键启动.bat             # 双击即用
│   └── 更新数据.bat             # 双击即更新
└── ...
```

---

## 七、开发计划（5 个阶段，每阶段都能独立验收）

| 阶段 | 做什么 | 验收标准（你能亲眼看到什么） |
|---|---|---|
| **阶段 1** | **数据引擎**：更新器 + 策略核心 + 数据库 | 双击一个脚本 → 屏幕上打印出"今日广度 = X 只"和候选股清单 |
| **阶段 2** | **后端服务**：Flask 接口 | 浏览器打开 `127.0.0.1:8000/api/today` → 看到 JSON 数据 |
| **阶段 3** | **前端看板**：今日广度 + 红绿灯 + 候选股表 | 浏览器打开首页 → 看到完整的"今天该做什么"看板 |
| **阶段 4** | **持仓与历史**：买入选股、止损提醒、广度历史曲线 | 能记录买入、看到止损线、看到广度走势图 |
| **阶段 5** | **自动化 + 说明书** | 15:30 自动更新；一份《使用手册》傻瓜照着做 |

**每一阶段做完，我都会让你实际跑一遍确认，再进入下一阶段。**

---

## 八、需要你拍板的 2 个决策

### 决策 1：门槛定多少？（关系到"高胜率"有多高）

你说"要保证高胜率才出手"。门槛越高，胜率越高，但机会越少：

| 门槛 | 六年胜率 | 六年出手次数 | 年均 | 对 2026 年 |
|---|---|---|---|---|
| 广度 ≥ 50 | 85.6% | 22 次 | 3.7 次 | 会触发 4 次 |
| 广度 ≥ 100 | 87.8% | 15 次 | 2.5 次 | 会触发 3 次 |
| 广度 ≥ 150 | **93.1%** | 8 次 | 1.3 次 | 只会触发 1 次 |

### 决策 2：工具站要做到什么程度？

- **A 精简版**：只看"今日广度 + 候选股 + 该不该动手"（阶段 1–3）
- **B 完整版**：再加上"持仓记录 + 止损提醒 + 历史走势"（阶段 1–5）

---

## 九、成本与风险

| 项目 | 说明 |
|---|---|
| 成本 | **¥0**（本地运行，无服务器/域名/数据库费用） |
| 数据 | akshare + 新浪源，免费；每日更新约 15 分钟 |
| 风险 | 新浪源若变更接口，需替换数据源（已有东财备用方案） |
| 隐私 | 全部数据在本机，不上传任何地方 |
| 声明 | 工具仅提供数据参考，不构成投资建议 |
