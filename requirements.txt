# ============================================================
#  乖离率超跌反弹策略工具 · Python 依赖清单
# ============================================================
#
#  安装方式（在本文件所在目录执行）：
#      python -m pip install -r requirements.txt
#
#  或者：直接双击 webapp/控制台.bat ，它会自动完成安装。
#
#  适用版本：Python 3.9 或更高（开发/验证环境为 Windows + Python 3.13）
#  本清单在以下版本上实测通过，写成 >= 是为了兼容更新版本：
#      pandas   3.0.5
#      numpy    2.5.3
#      flask    3.1.3
#      akshare  1.18.94
#      requests 2.34.2
#
#  说明：
#    * 行情数据获取用的是 akshare 的「新浪财经」接口，全部免费、无需注册。
#    * akshare 会自动带上 mini-racer（新浪接口解析数据要用），无需手写。
#    * 前端是手写的原生 HTML + Canvas，**不依赖任何外网 CDN**，断网也能看。
# ============================================================

pandas>=2.0
numpy>=1.24
flask>=3.0
akshare>=1.16
requests>=2.31
